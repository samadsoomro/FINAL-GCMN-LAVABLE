{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/public",
    "env": {
        "VERCEL_BUILD_SKIP_TYPE_CHECK": "1"
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/index.ts"
        },
        {
            "src": "/server/uploads/(.*)",
            "dest": "/api/index.ts"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ],
    "functions": {
        "api/index.ts": {
            "includeFiles": "server/**"
        }
    }
}